/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.16 public/models/Dino.gltf -o src/components/Dino.jsx -r public 
*/

import React, { useEffect, useRef } from "react";
import { useGLTF, useAnimations } from "@react-three/drei";

export function Dino({ hovered, ...props }) {
  const group = useRef();
  const { nodes, materials, animations } = useGLTF("/models/Dino.gltf");
  const { actions } = useAnimations(animations, group);
  console.log(actions);

  useEffect(() => {
    const anim = hovered ? "Yes" : "Idle";
    actions[anim].reset().fadeIn(0.5).play();
    return () => {
      if (actions[anim]) {
        actions[anim].fadeOut(0.5);
      }
    };
  }, [hovered]);

  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="CharacterArmature">
          <primitive object={nodes.Root} />
          <skinnedMesh
            name="Dino"
            geometry={nodes.Dino.geometry}
            material={materials.Atlas}
            skeleton={nodes.Dino.skeleton}
          />
        </group>
      </group>
    </group>
  );
}

useGLTF.preload("/models/Dino.gltf");
